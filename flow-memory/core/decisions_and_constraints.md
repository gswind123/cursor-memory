# 决策与约束

记录关键决策、架构 / 业务约束及「不可违背」规则；说明原因以便日后复审。

---

## 占位说明

每条建议包含：**决策或约束**、**背景 / 原因**、**影响范围**、**日期**（可选）。

### 示例条目

- **决策**：API 错误响应统一使用 `{ code, message }` 结构。
- **原因**：便于前端与监控一致处理。
- **范围**：新建服务端接口。
- **日期**：2030-01-01（占位）
