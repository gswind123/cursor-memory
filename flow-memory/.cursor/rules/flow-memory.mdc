---
alwaysApply: false
---

# flow-memory（分时记忆 · 公共契约）

本规则为 **manual-apply**（`alwaysApply: false`），内容为 **flow-memory** 体系的**唯一公共契约**。逐步检索顺序、输出格式与归档步骤不在此定义。

## 定位

- **flow-memory**：分时记忆原型（短期流水 / 中期核心 / 长期摘要）。
- 本规则只定义目录语义与共用边界；**何时读取或写入落盘**由对应技能（如 **`use-flow-memory`** / **`archive-flow-memory`**）另行约定，不在此重复。

## Root（可变锚点）

记忆落盘 **root** 由下方「候选 root 路径列表」解析得出（本规则的单一事实来源）；下文路径均相对于解析得到的 root。迁移项目时**只需增删或调整列表顺序**，不必重写本节其余文字。

### 候选 root 路径列表（自上而下优先）

路径均相对**当前 Cursor 工作区根**；每行一项，**从上到下**尝试：

```
./flow-memory/
./.workspace/flow-memory/
flow-memory/
.workspace/flow-memory/
```

可将自定义路径插入列表顶部或追加到底部；**禁止**在列表外臆测其它目录。

### 解析算法

1. 按列表顺序取下一项路径 \(p\)。
2. 若 \(p\) 在工作区内不存在或不是目录 → 跳过该项。
3. 若 \(p\) 满足下方「root 合法性检查」→ **选定 root = \(p\)**，结束解析。
4. 若列表耗尽仍无匹配 → **无效**：执行读写时必须中断并向用户报错（技能 **`use-flow-memory`** / **`archive-flow-memory`** 约定具体报错文案）；不得向列表外搜索或静默降级。

### root 合法性检查（flow-memory）

候选目录必须**同时**包含下列子路径（均为目录）才算有效 root：

- `core/`
- `episodic/`
- `summaries/`

任一缺失则该项不匹配，继续下一候选。

## 目录 schema（语义）

1. **Episodic** — `episodic/YYYY/YYYY-MM-DD.md`：短期工作流水（摘要级结论，非逐字对话）。
2. **Core** — 中期核心（修改需谨慎）：
   - `core/work_goals.md` — 当前阶段工作目标。
   - `core/decisions_and_constraints.md` — 决策与约束。
   - `core/project_context.md` — 业务与领域上下文。
   - `core/collaboration.md` — 协作偏好与边界。
   - `core/timeline.md` — 里程碑与时间线节点。
3. **Summaries** — `summaries/long_term.md`、`summaries/weekly.md`：长期滚动摘要。

根目录可选 **`recall_state.json`**：预留占位，默认不绑定独立召回流程。

## 共用禁止项（读与写）

- 不使用 `emotion` 或虚拟人式「关系叙事」字段。
- 不写入个人生活细节、敏感凭证、聊天逐字原文。
- 不依赖本契约未声明的其它规则或外部流程（无隐藏依赖）。
