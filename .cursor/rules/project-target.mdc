---
alwaysApply: true
---

# 仓库的目标

本仓库用于制作和完善两套专门提供给 cursor 使用的**用户级**记忆功能；

## 术语释义

### 记忆规则（本仓库中的实际所指）

- **以 Skills 为载体**：用户将本仓库 **`.agents/skills/`**（相对仓库根）中的技能目录同步到本机 `~/.agents/skills`（Windows：`%USERPROFILE%\.agents\skills`）。契约正文在技能的 **`SKILL.md`** 与同目录 **`reference.md`** 中。
- **Cursor User Rules** 更接近 Preferences 里一段**常驻提词**，不作为可被「引用」的公共契约库；落盘 root 与目录语义**不要**仅依赖 User Rules 承载。

### 记忆迭代

- 使用过程中，用户会要求使用记忆技能把聊天上下文或者指定内容记下来，也可能要求从过往记忆中拉起知识；这种工作流称为记忆迭代；

### 记忆原型

- 记忆迭代过程中有两个重要依赖：**记忆技能（契约）** 和 **记忆落盘文件**，这两个依赖整体被称为 **记忆原型**；
- 巧妙、高效的落盘目录结构与简洁、配套的技能契约，是高质量记忆原型的主要特征；

## 记忆库形式

两套记忆体系分别称为：

- **stack-memory**：分层知识库体系，追求在记忆迭代过程中形成分层 RAG 式知识库
- **flow-memory**：分时记忆体系，追求在记忆迭代过程中形成 "短期 - 长期 - 归档" 层次的类人记忆

## 记忆库使用方式

用户使用时把**技能**安装到 Cursor 用户全局 Skills，并在工作区放置落盘目录，即可启用：

1. 将 **`.agents/skills/`** 中相关技能同步到本机 `~/.agents/skills`（至少包含 **`search-memory-storage`** 与需要的 **`use-*` / `archive-*`**）。
2. 在用户工作区内建立落盘目录（常位于 `./flow-memory/`、`./stack-memory/` 或 `./.workspace/...`；是否被识别由技能解析）。
3. 通过编辑技能 **`search-memory-storage`** 内的「候选基准目录列表」调整 root 查找顺序（不要把路径契约只写在 User Rules 里）。

### 记忆技能 → 落盘文件的映射约定（避免歧义）

- **映射是什么**：技能必须明确「落盘根路径（root）」如何解析与其下的**固定目录结构**；agent 只按约定读写，不应猜测路径。
- **候选路径单一事实来源**：由技能 **`search-memory-storage`** 维护「候选基准目录列表 + 子目录名（`flow-memory` / `stack-memory`）」与解析算法；用户迁移工作区时通常只需**增删或调整该列表中的行、或调整顺序**。
- **默认推荐锚点**：以当前工作区为根，使用相对路径；默认列表已覆盖常见 `./`、`./.workspace/`、`(empty)`、`.workspace/` 与 segment 的组合。
- **最小可变项**：其余结构（`episodic`/`core`/`summaries` 或 L0/L1/L2/L3）保持不变；root 解析失败时：**`archive-*`** 须中断并报错、不写入；**`use-*`** 静默当作无记忆并继续主任务（见各技能）。

### 记忆落盘文件示例

假设用户正在某代码仓库中工作（即用 Cursor 打开该文件夹）。下列操作要求用户**自己手动遵循**，本项目产出的记忆原型中不包含这些工作：

- 用户需要建立一个满足要求的文件夹，例如 `./.workspace/`
- 用户需要在自身项目的版本控制中排除它，例如在 `.gitignore` 中添加 `/.workspace/`
- 用户需要把记忆模板目录放到工作区内（如 `./flow-memory/` 或 `./.workspace/flow-memory/`）
- 用户需要编辑已安装的 **`search-memory-storage`** 技能内的候选列表，使解析命中上述落盘根

## 本项目的长期目标

**这里是本项目所有任务的最终工作目标**

本项目追求制作两套记忆体系的高质量**记忆原型**

- 持续打磨技能契约：**`SKILL.md`** 简洁有效，流程完备；**`reference.md`** 承载目录语义且不重复 root 列表
- 两套记忆需要能**共存**，注意在命名上做出区分
- **格外注意**：技能安装在 Cursor 用户全局目录，记忆落盘在用户工作区内；同一套技能应能映射到不同工作区（主要靠 **`search-memory-storage`** 列表顺序）
- 对于记忆原型的落盘文件，打磨最适合的初版，确保各层 index 简洁清晰，初始 content 不产生误导
