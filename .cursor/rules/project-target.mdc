---
alwaysApply: true
---

# 仓库的目标

本仓库用于制作和完善两套专门提供给 cursor 使用的**用户级**记忆功能；

## 术语释义

### 记忆规则
 - 使用时，要求用户手动把 rules 和 skills 添加到 cursor 的 "User Rules & Skills"中，和具体项目无关，介绍对应记忆库的结构和使用方式；

### 记忆迭代
 - 使用过程中，用户会要求使用记忆规则中的技能把聊天上下文或者指定内容记下来，也可能要求从过往记忆中拉起知识；这种工作流称为记忆迭代；

### 记忆原型
 - 记忆迭代过程中有两个重要依赖：记忆规则 和 记忆落盘文件，这两个依赖整体被称为 **记忆原型**；
 - 巧妙、高效的记忆落盘文件形式和简洁、配套的记忆规则，是高质量记忆原型的主要特征；

## 记忆库形式

两套记忆体系分别称为：
 - stack-memory : 分层知识库体系，追求在记忆迭代过程中形成分层 RAG 式知识库
 - flow-memory : 分时记忆体系，追求在记忆迭代过程中形成 "短期 - 长期 - 归档" 层次的类人记忆

 ## 记忆库使用方式

用户使用时只要把记忆原型注册在 cursor 和项目空间内，即可启用，具体形式如下：
 1. 在 cursor 的 "User SKills & Rules" 中添加对应的skill和rule（即记忆规则）
 2. 在用户自身的知识库下，要求用户建立一个 "被版本控制忽略，但被 agent 识别"的文件夹，把记忆落盘文件放在其中
 3. 由用户适当调整记忆规则，令规则能以**智能化**的形式被匹配到对应的落盘点上

#### 记忆规则 → 落盘文件的映射约定（避免歧义）

- **映射是什么**：记忆规则/技能中必须明确「落盘根路径（root）」与其下的**固定目录结构**；agent 只按约定读写，不应猜测路径。
- **默认推荐锚点**：以当前工作区为根，使用相对路径（示例：`./.workspace/flow-memory/`、`./.workspace/stack-memory/`）。
- **最小可变项**：当迁移到另一个工作区时，尽量只需要调整**一处**“落盘根路径（root）”的取值；其余结构（如 episodic/core/summaries 或 L0/L1/L2/L3）保持不变。

#### 记忆落盘文件示例

假设用户正在 C# 代码仓库 'E:\\projects\\SampleCode' 中工作（即用cursor打开这个文件夹）
注意下列操作都要求用户**自己手动遵循**，本项目产出的记忆原型中不包含这些工作：
 - 用户需要建立一个满足要求的文件夹，比如 : './.workspace/'（绝对路径是'E:\\projects\\SampleCode\\.workspace'）
 - 用户需要在自身项目的版本控制中排除它，比如在 '.gitignore' 中添加 '/.workspace/'
 - 用户需要把记忆原型中的落盘文件放在'.workspace'下，比如 '.workspace/flow-memory/'
 - 用户需要手动在记忆规则中把**落盘根路径**指向该路径：假设对应的 mdc 为 `flow-memory.mdc`，则需要在文件中对应位置把“记忆落盘根目录”的取值改为 `.workspace/flow-memory/`

 ## 本项目的长期目标

**这里是本项目所有任务的最终工作目标**
本项目追求制作两套记忆体系的高质量**记忆原型**
 - 持续打磨记忆规则部分，对于 mdc 部分要求简洁有效，对于 skill 部分要求设计合理、功能完备
 - 两套记忆规则需要能**共存**，注意在命名上做出区分
 - **格外注意**：记忆规则保存在 cursor 的用户层（在preference里），记忆落盘在用户工作区内，尽量让用户能轻松地调整记忆规则，使得同一套记忆规则能对应到不同的工作区
 - 对于记忆原型的落盘文件，打磨最适合的初版，确保各层index简洁清晰，初始content不产生误导