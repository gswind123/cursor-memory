---
alwaysApply: false
---

# stack-memory（分层记忆 · 公共契约）

本规则为 **manual-apply**（`alwaysApply: false`），内容为 **stack-memory** 体系的**唯一公共契约**。写入落点表、索引同步步骤等不在此定义。

## 定位

- **stack-memory**：分层记忆原型（RAG 式知识库）；不作长篇正文仓库；不引入 persona/world 等契约外依赖。
- 在未满足 **读库闸门** 前，**禁止**读取、概括或引用本 root 下任意落盘内容，也不得臆测用户偏好。

## 读库闸门（Read Gate）

须**同时**满足：

1. **显式点名记忆**：用户消息含 `memory`、`记忆`、`偏好`、`风格`、`历史项目`、`以往习惯` 等一类用语，或明确要求「先读记忆再回答」「参考我的记忆」。
2. **指向本体系**：用户明确要求参考 **stack-memory**、**分层记忆**，或语境明确针对 L0–L3 / INDEX 式分层检索。

未同时满足：**禁止**读库。

## 写库触发（Write Trigger）

用户**明确表达**写入本体系的意图时方可写入（例如「归档到 stack-memory」「写入分层记忆」「记入 L1」「更新工作目标 / 决策 / 项目上下文」「加 L2 示例」「坑写到 L3」）。未表达归档意图则**不得**写入。

## Root（可变锚点）

- 落盘 **root** 默认目录名为 **`stack-memory/`**（相对当前 Cursor 工作区根）；下文路径均相对于该 root。
- 迁移时仅调整 root 锚点，**不改变** `L0_system_rules/`、`L1_project_knowledge/`、`L2_examples/`、`L3_feedback/` 及根级 **`INDEX.md`** 的职责约定。与工作区映射的总原则见仓库说明 **project-target**（本规则不以路径指向该文件）。

## 分层 schema 与权力顺序

- **L0_system_rules/**：硬规则（格式/契约/不可违背）。
- **L1_project_knowledge/**：方法论、参数语义、组合模式；工作助手主写入文件包括 `work_goals.md`、`decisions_and_constraints.md`、`project_context.md`。
- **L2_examples/**：极简示例（约 1～3 行/段）；**不得**推翻 L0/L1。
- **L3_feedback/**：踩坑与反例（现象→原因→修正）；不作风格来源，**不得**推翻 L0/L1。

权力顺序：**L0 > L1 > L2 > L3**。

**检索习惯（契约层）**：先读 root **`INDEX.md`** 再跟链接条目；单次任务少引用，避免递归扫全库。

## 共用禁止项（读与写）

- **默认不写 L0**：除非用户明确要求新增硬规则且具备可引用证据/契约；不确定时在 L1 记「待升格」而非写入 L0。
- 禁写：敏感凭证、个人生活、聊天逐字长文、无法泛化的一次性闲话。
- 不依赖本契约未声明的其它规则或外部流程。
