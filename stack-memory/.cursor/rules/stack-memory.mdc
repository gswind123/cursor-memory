---
alwaysApply: false
---

# stack-memory（分层记忆 · 公共契约）

本规则为 **manual-apply**（`alwaysApply: false`），内容为 **stack-memory** 体系的**唯一公共契约**。写入落点表、索引同步步骤等不在此定义。

## 定位

- **stack-memory**：分层记忆原型（RAG 式知识库）；不作长篇正文仓库；不引入 persona/world 等契约外依赖。
- 本规则只定义目录语义、权力顺序与共用边界；**何时读取或写入落盘**由对应技能（如 **`use-stack-memory`** / **`archive-stack-memory`**）另行约定，不在此重复。

## Root（可变锚点）

记忆落盘 **root** 由下方「候选 root 路径列表」解析得出（本规则的单一事实来源）；下文路径均相对于解析得到的 root。迁移项目时**只需增删或调整列表顺序**，不改变 `L0_system_rules/`、`L1_project_knowledge/`、`L2_examples/`、`L3_feedback/` 及根级 **`INDEX.md`** 的职责约定。

### 候选 root 路径列表（自上而下优先）

路径均相对**当前 Cursor 工作区根**；每行一项，**从上到下**尝试：

```
./stack-memory/
./.workspace/stack-memory/
stack-memory/
.workspace/stack-memory/
```

可将自定义路径插入列表顶部或追加到底部；**禁止**在列表外臆测其它目录。

### 解析算法

1. 按列表顺序取下一项路径 \(p\)。
2. 若 \(p\) 在工作区内不存在或不是目录 → 跳过该项。
3. 若 \(p\) 满足下方「root 合法性检查」→ **选定 root = \(p\)**，结束解析。
4. 若列表耗尽仍无匹配 → **无效**：执行读写时必须中断并向用户报错（技能 **`use-stack-memory`** / **`archive-stack-memory`** 约定具体报错文案）；不得向列表外搜索或静默降级。

### root 合法性检查（stack-memory）

候选目录必须**同时**包含：

- 根下文件 **`INDEX.md`**
- 目录 **`L1_project_knowledge/`**

任一缺失则该项不匹配，继续下一候选。

## 分层 schema 与权力顺序

- **L0_system_rules/**：硬规则（格式/契约/不可违背）。
- **L1_project_knowledge/**：方法论、参数语义、组合模式；工作助手主写入文件包括 `work_goals.md`、`decisions_and_constraints.md`、`project_context.md`。
- **L2_examples/**：极简示例（约 1～3 行/段）；**不得**推翻 L0/L1。
- **L3_feedback/**：踩坑与反例（现象→原因→修正）；不作风格来源，**不得**推翻 L0/L1。

权力顺序：**L0 > L1 > L2 > L3**。

**检索习惯（契约层）**：先读 root **`INDEX.md`** 再跟链接条目；单次任务少引用，避免递归扫全库。

## 共用禁止项（读与写）

- **默认不写 L0**：除非用户明确要求新增硬规则且具备可引用证据/契约；不确定时在 L1 记「待升格」而非写入 L0。
- 禁写：敏感凭证、个人生活、聊天逐字长文、无法泛化的一次性闲话。
- 不依赖本契约未声明的其它规则或外部流程。
