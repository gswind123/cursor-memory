# 当前工作目标（L1 · 可变）

记录本阶段优先工作目标；内容**变化频繁**，可与 `flow-memory` 的 `work_goals` 呼应（分层侧侧重「与模式/规范可对齐的目标陈述」）。

---

## 占位说明

以下为字段示例，初次填写或后续维护时替换。

### 当前焦点

- **目标**：一句话可验收结果。
- **周期**：如「本周」「Sprint N」。
- **完成标准**：可验证条件。

### 历史摘要（可选）

- 仅保留最近 1～2 个阶段的**一行**摘要，避免 L1 膨胀。
